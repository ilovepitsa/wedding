services:
  backend:
    build: ./backend
    env_file: .env
    restart: unless-stopped
    expose:
      - "8080"

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
